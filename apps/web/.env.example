# Base URL of the HeySnap cloud API.
# In dev this is set automatically by the `dev` script.
VITE_CLOUD_SERVER_URL=http://localhost:4100

# Sarvam speech-to-text API key (used for the voice grip button).
VITE_SARVAM_API_KEY=
