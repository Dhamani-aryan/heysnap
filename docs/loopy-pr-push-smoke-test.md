# Loopy PR Push Smoke Test

This temporary document verifies that pushing a new commit to an open pull
request launches the configured Codex review task for this repository.

The follow-up commit is the `pull_request.synchronize` test event.
